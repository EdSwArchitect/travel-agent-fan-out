[project]
name = "travel-agents"
version = "0.1.0"
description = "Parallel multi-agent travel demo using the OpenAI Agents SDK"
requires-python = ">=3.11"
dependencies = [
  "openai-agents",
  "pydantic>=2",
  "python-dotenv>=1",
]

[tool.uv]
package = false
